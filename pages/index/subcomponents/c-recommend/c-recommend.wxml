<view class="c-recommend">
	<van-grid border="">
		<van-grid-item use-slot wx:for="{{recommends}}" wx:key="unique">
			<van-image width="160rpx" height="160rpx" 
      src="{{item.image}}" 
			bind:load="imageLoad"/>
			<text>{{item.title}}</text>
		</van-grid-item>
	</van-grid>
</view>