<view class="c-tab-menu">
		<van-sidebar custom-class="van-sidebar"
								 active-key="{{activeKey}}"
								 bind:change="onChange">
			<block wx:for="{{categories}}" wx:key="unique">
				<van-sidebar-item title="{{item.title}}" />
			</block>
		</van-sidebar>
</view>